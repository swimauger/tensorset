{
  "name": "tensorset",
  "author": "Mark Auger",
  "version": "1.1.1",
  "description": "TensorflowJS Dataset parsing and stringifying",
  "main": "lib/Tensorset.js",
  "type": "commonjs",
  "license": "MIT",
  "scripts": {
    "prepublish": "tsc",
    "build": "tsc && cp test/dataset.json lib/test",
    "dev": "mkdir -p lib/test && cp test/dataset.json lib/test && tsc --watch",
    "clean": "rm -rf lib",
    "test": "jest --silent"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/swimauger/tensorset.git"
  },
  "keywords": [
    "Tensorflow",
    "KNN Classifier",
    "Dataset",
    "Save",
    "Load",
    "Stringify",
    "Parse"
  ],
  "bugs": "https://github.com/swimauger/tensorset/issues",
  "homepage": "https://github.com/swimauger/tensorset#readme",
  "dependencies": {
    "@tensorflow/tfjs": "^2.0.1",
    "tsconfig.json": "^1.0.7",
    "yarn.lock": "^0.0.1-security"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.2"
  }
}
